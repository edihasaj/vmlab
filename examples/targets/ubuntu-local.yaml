name: ubuntu-local
transport: crabbox
tags: [linux, local, vm]
crabbox:
  id: ubuntu-local        # crabbox lease id or friendly slug (-> crabbox -id)
capabilities:
  shell: true
  sync: true
  install: true
  screenshot: true
